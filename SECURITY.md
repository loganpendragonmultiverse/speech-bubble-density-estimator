# Security

Report vulnerabilities through GitHub private vulnerability reporting. Images and CBZ archives are untrusted. The scanner applies archive path, entry-count, and uncompressed-size limits, decodes supported images in memory, never executes embedded content, and writes no extracted pages. Image libraries can still contain vulnerabilities, so keep dependencies current.
